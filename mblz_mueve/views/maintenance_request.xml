<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>


        <!-- Inherit Form View to Modify it -->
        <record id="ticket_hr_equipment_request_view_form" model="ir.ui.view">
            <field name="name">ticket.equipment.request.form</field>
            <field name="model">maintenance.request</field>
            <field name="inherit_id" ref="maintenance.hr_equipment_request_view_form"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='email_cc']" position="after">
                    <!-- Add your fields or attributes here -->
                    <field name="ticket_id" attrs="{'invisible': [('ticket_id', '=', False)]}" readonly="1"/>
                    <field name="msj_solution_close"
                           attrs="{'invisible': [('ticket_id', '==', False)]}"/>
                    <field name="create_type" invisible="1"/>
                    <field name="datetime_create_ot" invisible="1"/>
                </xpath>

                <xpath expr="//field[@name='employee_id']" position="attributes">
                    <attribute name="domain">[('can_create_ot', '=', True)]</attribute>
                    <attribute name="options">{'no_open': False, 'no_create': True, 'no_create_edit': True}</attribute>
                </xpath>

            </field>
        </record>
    </data>
</odoo>