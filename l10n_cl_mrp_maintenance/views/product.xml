<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Inherit Form View to Modify it -->
        <record id="mblz_product_template_only_form_view" model="ir.ui.view">
            <field name="name">mblz_product.template.product.form</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_only_form_view"/>
            <field name="arch" type="xml">

                <xpath expr="//notebook" position="inside">
                    <!-- Add your fields or attributes here -->
                    <page name="page_mtto" string="MTTO">
                        <group>
                            <field name="me_category_ids"
                                   options="{'no_create': True, 'no_create_edit': True, 'no_open': False}"
                                   domain="[('id', '!=', id)]"
                                   widget="many2many_tags"/>
                        </group>
                    </page>
                </xpath>

            </field>
        </record>


        <record id="mblz_product_normal_form_view" model="ir.ui.view">
            <field name="name">mblz_product.product.form</field>
            <field name="model">product.product</field>
            <field name="inherit_id" ref="product.product_normal_form_view"/>
            <field name="arch" type="xml">

                <xpath expr="//notebook" position="inside">
                    <!-- Add your fields or attributes here -->
                    <page name="page_mtto" string="MTTO">
                        <group>
                            <field name="me_category_ids"
                                   options="{'no_create': True, 'no_create_edit': True, 'no_open': False}"
                                   domain="[('id', '!=', id)]"
                                   widget="many2many_tags"/>
                        </group>
                    </page>
                </xpath>

            </field>
        </record>



    </data>
</odoo>